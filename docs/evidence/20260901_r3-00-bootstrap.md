# R3-00 bootstrap evidence — attached-device baseline

Goal: `goal-adaptive-agent-r3-greenfield-native-ios-runtime-v0-20260901`
Task: R3-00 Repository/bootstrap boundary
Date: 2026-09-01
Repository: `<repository-root>`
(new git repository, branch `main`, created by this task; working tree left
uncommitted for the orchestrator's atomic wave checkpoint)

## Intended host OS

- Windows desktop host (this development machine):
  `platform.platform()` = `Windows-11-10.0.26200-SP0`.
- CPython 3.12.10; git 2.54.0.windows.1; pip works with direct git
  dependencies (network OK).
- Repo-local venv: `.venv` (`.venv\Scripts\python.exe`), Python 3.12.10.

## Target device class

- Physical iPhone, previously paired with this host through the standard
  Apple Lockdown pairing flow (a host-side pair record exists).
- Privacy rule: the device is referenced here only in redacted generic terms.
  No raw UDID, serial, ECID, MAC, or pair-record identifier is recorded in
  this repository, and ordinary `status()`/trace output must never expose
  them either.
- Exact device model/iOS version will be recorded (redacted) during the
  R3-02/R3-08 device smoke when attached; this bootstrap task does not assert
  them.

## Intended normal transport

- iOS 17+ in-process RSD userspace tunnel via unmodified upstream
  `pymobiledevice3` `PreferredRsdTunnel` (current upstream Python API,
  in-process — no subprocess, no CLI).
- A separate persistent `tunneld` process is **not** a normal R3 requirement;
  operation that historically depended on one is reference material only.

## First WDA runner strategy (R3 smoke)

- Use the WebDriverAgent runner app **already installed on the paired
  device**; no custom runner build step is introduced by bootstrap.
- Launch/attach it via upstream WDA service APIs (upstream
  `WdaServiceClient` over lockdown/usbmux services) — no downstream
  `wda_ext.py` extensions, no legacy API shapes.
- The concrete runner bundle-id selection (and any install/update decision)
  is deferred and recorded as an **R3-08 decision**; this task fixes only the
  strategy: existing installed runner + upstream service APIs.

## Dependency evidence

See `docs/PROVENANCE.md` for the full record:

- `pymobiledevice3` 11.3.0 installed from
  `git+https://github.com/doronz88/pymobiledevice3@ec4ac06a850a6a884ca778350621f354faf347c6`
  (`pip show`, `pip freeze`, and `direct_url.json` all confirm the exact
  pinned upstream commit; installed location is this repo's `.venv`, not a
  downstream-fork path).
- License: canonical GPLv3 text fetched from gnu.org via Python `urllib` into
  `LICENSE` (35,149 bytes).

## Checks run (R3-00)

| Check | Command | Result |
|---|---|---|
| Provenance scan | `.venv\Scripts\python scripts\check_provenance.py` | `OK: no Phone Harness import/name/dependency in production paths (src/, pyproject.toml).` exit 0 |
| Package import | `.venv\Scripts\python -c "import adaptive_agent.ios_runtime"` | `import-ok` |
| Deterministic suite | `.venv\Scripts\python -m pytest -q` | `3 passed in 0.11s` exit 0 |
| Upstream pin proof | `pip freeze` / `direct_url.json` | exact commit `ec4ac06a850a6a884ca778350621f354faf347c6` |

Notes from execution:

- The provenance guard caught one docstring mention of the forbidden harness
  name inside `src/adaptive_agent/ios_runtime/__init__.py`; the source was
  reworded (guard not weakened).
- `xonsh` (transitive dependency of `pymobiledevice3`) auto-registers a
  pytest11 plugin that crashes under non-console pytest runs on Windows; the
  repo disables exactly that plugin with `addopts = "-p no:xonsh"` in
  `pyproject.toml` (repo-local config, not an upstream patch). Details in
  `docs/PROVENANCE.md`.

## Scope

Bootstrap only: package skeleton (`src/adaptive_agent/` +
`src/adaptive_agent/ios_runtime/__init__.py`), packaging metadata,
LICENSE/README/.gitignore, provenance guard, docs. Runtime modules
(`models.py`, `transport.py`, `runtime.py`, ...) are owned by later R3
slices and intentionally not pre-created.
