# R4-06 — required-behavior comparison (Praxiom vs historical reference)

- Date: 2026-09-05 (UTC)
- Sensitivity: **PUBLIC**
- Authority for required behavior: R2 contract
  (`20260831_r2-native-ios-runtime-contract.md`); current Praxiom behavior is
  authoritative over historical behavior. Historical Phone Harness material is
  reference evidence only — no import, subprocess, MCP hop, fallback, shim, or
  copied production source was used in any device run.
- Device evidence: `docs/evidence/20260905_r4-real-device-acceptance.md`
  (this run) plus `docs/evidence/20260903_r3-08-attached-device-pass.md` and
  `docs/evidence/20260903_praxiom-rename.md` (prior attached-device smokes).
- Deterministic evidence: R2-A01..A10 fixture suite (fake transport), green in
  the default phone-independent `pytest` run.

Verdict vocabulary (frozen): `equivalent-required` | `Praxiom-safer` |
`legacy-only-not-required` | `gap` | `environment-not-exercised`.

## Capability map

| R2 item | Required behavior | Praxiom device evidence | Historical reference | Verdict |
|---|---|---|---|---|
| 4.1 `status` / R2-A01 | side-effect-free health/capability snapshot, no identifiers | pre/post `DISCONNECTED`->`READY`->`CLOSED` enum transitions; capabilities + `max_batch_actions` exposed; zero identifiers in output | historical status/health reads were evidence for the privacy rule, not an API target | `equivalent-required` |
| 4.2 `observe` / R2-A02 | one capture, fresh opaque revision, revision-bound refs, shared screenshot-pixel space | repeated rounds: unique revisions, 1206x2622 consistency, bounded element counts; stale refs rejected with zero device calls | legacy capture paths informed safety rules only | `equivalent-required` |
| 4.3 `execute` valid batch / R2-A03 | bounded ordered execution, revision invalidated, counts/timings without payloads | all 7 primitives + ordered 2-action batch on hardware; invalidation per mutation; trace carries kinds/durations only | same | `equivalent-required` |
| 4.3 preflight: stale revision / R2-A04 | reject before first side effect | hardware counter proof: zero device calls | same | `equivalent-required` |
| 4.3 preflight: invalid later action / R2-A05 | whole batch rejected before first side effect | hardware counter proof (out-of-bounds later action, `INVALID_REQUEST`, zero calls) | same | `equivalent-required` |
| 4.3 preflight: foreign ref / R2-A09 | reject before device call | hardware counter proof (`STALE_REVISION`, zero calls) | same | `equivalent-required` |
| 4.3 preflight: over-limit batch / R2-A10 | reject before device call | hardware counter proof (33 > 32, `INVALID_REQUEST`, zero calls) | same | `equivalent-required` |
| 4.3 mandatory revision for every batch | no bypass, incl. home/launch | every device batch carried the exact current revision; missing/stale rejected | legacy convenience paths without revision binding are superseded | `Praxiom-safer` |
| 5.2 ambiguous failure / R2-A06 | `EFFECT_UNKNOWN`, `retry_safe=false`, no replay, reconcile by fresh observe | deterministic fake proof; live ambiguous effect honestly `environment-not-exercised` (never manufactured) | legacy retry behavior informed the no-blind-replay rule | `environment-not-exercised` (live path only) |
| 4.5 `recover` / R2-A07 | rebuild plumbing only, invalidate revision, replay zero actions | device proof: `repaired=true`, trace `execute` counter unchanged across `recover`, reconciled observe afterwards | same | `equivalent-required` |
| 4.6 `close` / R2-A08 | idempotent, owned resources only, post-close rejections | double close clean, `CLOSED` status, 3x `RUNTIME_CLOSED` rejections; owned handles only | same | `equivalent-required` |
| A11 real-device path, upstream-only | screenshot/accessibility/action on hardware with no Phone Harness edge | full matrix on USB + in-process `RSD_USERSPACE` + WDA, provenance guard green | legacy execution path is the thing being migrated away from | `equivalent-required` |
| 4.4 `invalidate(reason)` bound | machine-oriented bounded reason, traceable | 128-char ASCII-printable bound enforced before side effects; raw reason never enters trace, only a Runtime-local keyed fingerprint is retained for correlation | no legacy equivalent required; stricter than any historical behavior | `Praxiom-safer` |
| §7 shared-revision optimization | screenshot + accessibility may share one revision | every capture returns both sources under one revision token | same | `equivalent-required` |
| OCR / visual recognition | explicitly not owned (§2) | not implemented, not exercised | any legacy OCR behavior is `legacy-only-not-required` | `legacy-only-not-required` |
| display-name-to-app resolution | explicitly not owned (§2, §4.3) | `launch_app` takes bundle id only | any legacy name-heuristics behavior is `legacy-only-not-required` | `legacy-only-not-required` |
| domain/game/purchase/Teaching/Knowledge policy | explicitly not owned (§2) | none implemented; device actions stayed in Settings/Home | same | `legacy-only-not-required` |
| pre-iOS-17 LOCKDOWN path | outside transport-v0 target scope unless required | target environment is iOS 17+ RSD; no LOCKDOWN requirement exists for this acceptance | historical path only | `legacy-only-not-required` |
| Wi-Fi transport | outside transport-v0 scope unless the target environment requires it | USB satisfies the accepted target environment; no Wi-Fi requirement was triggered | historical/optional path only | `legacy-only-not-required` |

## Remediation / evidence references

- No `gap` verdicts: every `gap` cell would require an exact remediation
  reference; none is owed.
- `environment-not-exercised` (live ambiguous effect): remediation is
  re-running the R4-04 device cell if an ambiguous failure ever occurs
  naturally, classifying it per §5.2, and reconciling by fresh observe. It
  must not be manufactured by replaying a non-idempotent action. Reference:
  deterministic A06 fixtures + this report's R4-04 section.
- `environment-not-exercised` (hotplug, disconnect):
  remediation is re-probing when the physical condition is safely available;
  until then these are honest coverage limits, not runtime defects.
- LOCKDOWN and Wi-Fi are not unresolved R4 environment cells: the frozen plan
  explicitly excludes Wi-Fi unless required by the target environment, and
  the accepted target is iOS 17+ RSD. They are `legacy-only-not-required`.

## Conclusion

Required R2 behavior on real hardware is `equivalent-required` or
`Praxiom-safer` in every exercised cell; legacy-only behaviors remain
non-requirements per R2 §2; the three R4 matrix environment exceptions are
hotplug, live ambiguity, and live disconnect, each with exact remediation.
LOCKDOWN and Wi-Fi are outside the accepted target scope. No contract change is
justified by this comparison: the six-operation boundary stands.
