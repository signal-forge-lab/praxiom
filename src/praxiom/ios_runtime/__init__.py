"""Praxiom Native iOS Runtime v0.

Greenfield, independently implemented iOS device runtime built directly on
unmodified upstream ``pymobiledevice3``. The enforced provenance boundary and
the public contract (status / observe / execute / invalidate / recover /
close) are documented in ``docs/PROVENANCE.md``.
"""
